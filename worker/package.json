{
  "name": "gtm-live-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker — CORS proxy for the news ticker's 4 no-CORS RSS feeds. Nothing else.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260718.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.112.0"
  }
}
